{
  "name": "chatify",
  "version": "1.0.0",
  "description": "Chatify is a messenging app with some major functionalities.",
  "main": "index.js",
  "scripts": {
    "build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build",
    "start": "npm run start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "nodemailer": "^8.0.1"
  }
}
